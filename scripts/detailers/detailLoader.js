import { loadDetails as details3a } from './details3A.js'
import { loadDetails as details6 } from './details6.js'

export const loadAllDetails = (scene) => {
    details3a(scene);
    details6(scene);
}